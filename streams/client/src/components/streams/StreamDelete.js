import React from 'react';
import Modal from '../Modal/Modal';

const StreamDelete = () => {
  return (
    <div>
      <h2> Stream Delete</h2>
      <Modal />
    </div>
  );
};

export default StreamDelete;
